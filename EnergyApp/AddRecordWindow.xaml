<Window x:Class="EnergyApp.AddRecordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Добавить запись" Height="300" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Дата:"/>
            <DatePicker x:Name="DatePicker"
						SelectedDate="{x:Static sys:DateTime.Today}"
						DisplayDateEnd="{x:Static sys:DateTime.Today}" />
        </StackPanel>

        <StackPanel Margin="0,60,0,10">
            <TextBlock Text="Потребление (кВт⋅ч):"/>
            <TextBox x:Name="ConsumptionTextBox"/>
        </StackPanel>

        <StackPanel Margin="0,120,0,10">
            <TextBlock Text="Комментарий:"/>
            <TextBox x:Name="CommentTextBox"/>
        </StackPanel>

        <Button Content="Добавить" Grid.Row="3" Height="35" Width="100" HorizontalAlignment="Right" Click="AddButton_Click"/>
    </Grid>
</Window>